# Servidor
PORT=3000
NODE_ENV=development

# Stripe (OBRIGATÓRIO)
STRIPE_SECRET_KEY=  # Sua chave secreta do Stripe
STRIPE_WEBHOOK_SECRET=  # Endpoint secret do webhook

# Email SMTP (OBRIGATÓRIO)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Supabase (SE USAR)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=